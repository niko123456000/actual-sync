# Future considerations

- **Web interface:** A small web UI that lists Redbark and Actual Budget account IDs and helps users set up the account mapping (e.g. pick Redbark account → pick Actual account, then export as config or paste into add-on Configuration).
