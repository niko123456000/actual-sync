# Future considerations

- ~~**Web interface:** A small web UI that lists Redbark and Actual Budget account IDs and helps users set up the account mapping.~~ Done: run `--export-accounts` (or `./scripts/setup-account-ids.sh --export`), then open `web/account-mapping.html`, paste JSON, pair accounts, and copy the mapping.
